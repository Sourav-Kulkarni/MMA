# Core
mkdocs>=1.6.0

# Theme — Material for MkDocs (best-in-class, mobile-friendly)
mkdocs-material>=9.5.0

# Plugins
mkdocs-glightbox>=0.4.0          # Lightbox for images/GIFs
mkdocs-git-revision-date-localized-plugin>=1.2.0  # "Last updated" timestamps

# Markdown extensions (mostly bundled with Material, listed for clarity)
pymdown-extensions>=10.7.1       # Admonitions, tabs, keys, emoji, etc.
